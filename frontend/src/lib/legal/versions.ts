import type { LegalDocumentKey } from './types';

/**
 * Редакции юридических документов.
 *
 * ⚠️ Должны совпадать с `backend/src/modules/legal/versions.ts`. Один общий файл
 * невозможен: Docker собирает `frontend` и `backend` из РАЗНЫХ контекстов
 * (см. render.yaml), и файл из корня репозитория не попадёт ни в один образ.
 * Расхождение ловит `frontend/scripts/check-legal.ts` — гонять перед пушем.
 *
 * Поднимать версию нужно при СУЩЕСТВЕННОМ изменении текста: у пользователей,
 * согласившихся со старой редакцией, согласие станет устаревшим, и они увидят
 * окно с просьбой принять новую. Правка опечатки версию не меняет.
 */
export const DOCUMENT_VERSIONS: Record<LegalDocumentKey, string> = {
  TERMS: '1.0 · 26.06.2026',
  PRIVACY: '1.1 · 04.08.2026',
  COOKIES: '1.0 · 03.08.2026',
};
