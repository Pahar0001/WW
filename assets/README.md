# Assets

Brand and media assets live here. The reference build ships no binary images to
keep the repo light; the UI uses CSS, the generative canvas, and (optional) remote
images. Drop hero imagery into `frontend/public/` and reference by path.

Required (placeholders): `logo.svg`, `og-image.png`. Add real, license-clear media
before production.
