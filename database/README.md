# Database (reference copy)

The **canonical** Prisma schema lives at `backend/prisma/schema.prisma`.
`database/prisma/schema.prisma` is a synced copy for reference/diagramming.
See `docs/DATABASE.md` for the ER overview and migration notes.
